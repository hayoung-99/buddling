{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "//": "보안 헤더와 CSP 는 middleware.ts 로 옮겼다 — nonce 를 요청마다 발급해야 하므로 정적 헤더로는 안 된다. 여기 남은 것은 자산 캐시처럼 요청과 무관한 것뿐이다.",
  "headers": [
    {
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
